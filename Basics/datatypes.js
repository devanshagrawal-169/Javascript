// use as strict version of JS code
"use strict";

// alert(3 + 3); we are uising NOde js, not a browser

let name = "devansh";
let age = 18
let isLoggedin = false
let state;

let city = null

// number -> 2 to power some number
// string -> ""
// boolean -> true, false
// null -> standalone value
// undefined 
// symbol
// object

console.log(typeof(null))
console.log(typeof(undefined))
console.log(typeof(isLoggedin))